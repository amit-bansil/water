/*
 * DebugFileGUI.java
 * CREATED:    Jan 9, 2005 12:16:55 PM
 * AUTHOR:     Amit Bansil
 * PROJECT:    CELESTFramework
 * 
 * Copyright 2005 The Center for Polymer Studies,
 * Boston University, all rights reserved.
 * */
package cps.jarch.application.gui;

import javax.swing.JFrame;

/**
 * static subsystem for user to view/upload debug log
 * 
 * <P>
 * has different modes that are set by a syspref on startup
 * 
 * Usage: 1.if an error occurs before logging is on the starter should
 * catch+inform
 * <P>
 * user show splash screen
 * <P>
 * 2.start logging
 * <P>
 * 3.if the program didn't shutdown normally the user is prompted to send the
 * log
 * <P>
 * 4.app runs, logging does its thing
 * <P>
 * 5.on normal exit if log has errors prompt user to upload
 * <P>
 * 6.on normal exit kill log
 * 
 * @version 0.0
 * @author Amit Bansil
 */
public class ErrorFileGUI {
	/**
	 * enable logging at correct level (verbose,std.,none) based on syspref
	 * prompt user about errors during last execution for upload
	 * only do stuff on first call
	 */
	public static final void startup() {
		throw new UnsupportedOperationException();//TODO
	}
	
	/**
	 *  prompts user to upload log if the log has errors, then does it if the user wants
	 */
	public static final void shutdown() {
		throw new UnsupportedOperationException();//TODO
		
	}
	
	/**
	 * shows a window with the log contents in it.
	 * rem. to flush
	 * buttons: close, refresh, copy to clipboard, upload. also sys pref for
	 * log mode, warn user about detailed logging
	 */
	public static final void showDebugLog(JFrame parent) {
		throw new UnsupportedOperationException();//TODO

	}

	/**
	 * open webbrowser with bugreport form and log location pre entered
	 * as well as appname+version, ask for details, contact info,
	 * stress privacy
	 * rem to flush & copy log to seperate file to avoid errors with temp delete
	 */
	public static final void doUpload() {
		throw new UnsupportedOperationException();//TODO

	}
}
